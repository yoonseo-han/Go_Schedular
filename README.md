# Go_Schedular

A **toy scheduler** that loosely mimics Go’s **G–M–P** idea: goroutine-like tasks (**G**), worker loops (**M**), and logical processors (**P**) with local and global run queues plus simple work stealing.

## Run

```bash
go run .
```

